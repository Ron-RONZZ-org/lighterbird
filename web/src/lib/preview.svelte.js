/**
 * Preview utility — re-exported from @lightercore/ui.
 *
 * Provides:
 *   createPreviewState() — Reactive state for PreviewDialog
 *   showPreviewInTab(content, format) — Open rendered content in new tab
 *   PREVIEW_CSS — Default preview stylesheet
 */
export { createPreviewState, showPreviewInTab, PREVIEW_CSS } from "@lightercore/ui/preview.svelte.js";
